---
title: Text Field
description: Single-line text input primitive that owns the value, separates live changes from commit, exposes disabled/readOnly/invalid state, and wires label, description, and message parts together.
section: components
sidebar:
  order: 80
  group: Form
---

import { PackageMeta, PropTable, Callout, PrimitiveAnatomy , LoomPreview } from "@/components/docs/mdx";

<PackageMeta
  packageName="@lattice-ui/react-text-field"
  status="stable-direction"
  importName="TextField"
  dependencies={["@lattice-ui/react-runtime", "@lattice-ui/react-focus", "@lattice-ui/react-motion"]}
/>

Text Field wraps a Roblox `TextBox` for single-line input: it owns the value and separates a live
change from a commit, so your component decides when to validate or persist. Reach for it when an
input needs **controlled or uncontrolled value state**, a **change-versus-commit** split, and shared
**disabled / readOnly / required / invalid** state across a label, description and message. For
multi-line input, [Textarea](/lattice-ui/components/textarea/) is the sibling primitive.

## Preview

<LoomPreview scene="TextFieldExampleScene" height={310} />

## Import

```ts
import { TextField } from "@lattice-ui/react-text-field";
```

## Anatomy

Only `Root` and `Input` are required; `Label`, `Description` and `Message` read shared state from
context.

<PrimitiveAnatomy title="TextField anatomy">

```tsx title="TextField anatomy"
<TextField.Root>
  <TextField.Label />
  <TextField.Input />
  <TextField.Description />
  <TextField.Message />
</TextField.Root>
```

| Part | Required | Responsibility |
| --- | --- | --- |
| `TextField.Root` | yes | Owns the value, the commit callback, and shared disabled/readOnly/required/invalid state. |
| `TextField.Input` | yes | The `TextBox` that renders the value and reports changes, focus, and commit. |
| `TextField.Label` | no | A `textbutton` that focuses the input when activated. |
| `TextField.Description` | no | A static `textlabel` for helper text. |
| `TextField.Message` | no | A `textlabel` for validation text that recolors when `invalid` is set. |

</PrimitiveAnatomy>

## Examples

### Basic labeled field

An uncontrolled field seeded with `defaultValue`, so the Root keeps the value and you hear about it
through the callbacks. `Label`, `Input` and `Description` each take `asChild`; clicking the label
captures focus on the input.

```tsx title="DisplayNameField.tsx"
import { TextField } from "@lattice-ui/react-text-field";

export function DisplayNameField() {
  return (
    <TextField.Root defaultValue="" onValueCommit={(value) => print(`display name: ${value}`)}>
      <frame BackgroundTransparency={1} Size={UDim2.fromOffset(240, 88)}>
        <uilistlayout Padding={new UDim(0, 4)} SortOrder={Enum.SortOrder.LayoutOrder} />

        <TextField.Label asChild>
          <textbutton
            AutoButtonColor={false}
            BackgroundTransparency={1}
            Size={UDim2.fromOffset(240, 22)}
            Text="Display name"
            TextXAlignment={Enum.TextXAlignment.Left}
          />
        </TextField.Label>

        <TextField.Input asChild>
          <textbox PlaceholderText="Enter a name" Size={UDim2.fromOffset(240, 36)} TextXAlignment={Enum.TextXAlignment.Left}>
            <uipadding PaddingLeft={new UDim(0, 10)} PaddingRight={new UDim(0, 10)} />
          </textbox>
        </TextField.Input>

        <TextField.Description asChild>
          <textlabel
            BackgroundTransparency={1}
            Size={UDim2.fromOffset(240, 20)}
            Text="Shown to other players."
            TextXAlignment={Enum.TextXAlignment.Left}
          />
        </TextField.Description>
      </frame>
    </TextField.Root>
  );
}
```

### Controlled value

Pass `value` and `onValueChange` when something outside needs to read or set the text. Because the
owned value is mirrored onto the `TextBox`, setting state from outside updates the visible text too.

```tsx title="ItemSearchField.tsx"
import { useState } from "@rbxts/react";
import { TextField } from "@lattice-ui/react-text-field";

export function ItemSearchField(props: { onQueryChange: (query: string) => void }) {
  const [query, setQuery] = useState("");

  const updateQuery = (nextQuery: string) => {
    setQuery(nextQuery);
    props.onQueryChange(nextQuery);
  };

  return (
    <TextField.Root value={query} onValueChange={updateQuery}>
      <frame BackgroundTransparency={1} Size={UDim2.fromOffset(300, 36)}>
        <uilistlayout FillDirection={Enum.FillDirection.Horizontal} Padding={new UDim(0, 8)} />

        <TextField.Input asChild>
          <textbox PlaceholderText="Search items" Size={UDim2.fromOffset(240, 36)} TextXAlignment={Enum.TextXAlignment.Left}>
            <uipadding PaddingLeft={new UDim(0, 10)} PaddingRight={new UDim(0, 10)} />
          </textbox>
        </TextField.Input>

        <textbutton
          BackgroundColor3={Color3.fromRGB(59, 66, 84)}
          Event={{ Activated: () => updateQuery("") }}
          Size={UDim2.fromOffset(52, 36)}
          Text="Clear"
          TextColor3={Color3.fromRGB(236, 241, 249)}
        />
      </frame>
    </TextField.Root>
  );
}
```

### Change vs commit

`onValueChange` fires on every text change; `onValueCommit` fires once when editing ends. Use change
for live UI — previews, counters, filtering — and commit for the expensive work: saving, validation,
history.

```tsx title="ServerNameField.tsx"
import { useState } from "@rbxts/react";
import { TextField } from "@lattice-ui/react-text-field";

export function ServerNameField(props: { onSave: (name: string) => void }) {
  const [draft, setDraft] = useState("My Server");
  const [saved, setSaved] = useState("My Server");

  return (
    <TextField.Root
      value={draft}
      onValueChange={setDraft}
      onValueCommit={(committed) => {
        setSaved(committed);
        props.onSave(committed);
      }}
    >
      <frame BackgroundTransparency={1} Size={UDim2.fromOffset(240, 64)}>
        <uilistlayout Padding={new UDim(0, 4)} SortOrder={Enum.SortOrder.LayoutOrder} />

        <TextField.Input asChild>
          <textbox Size={UDim2.fromOffset(240, 36)} TextXAlignment={Enum.TextXAlignment.Left}>
            <uipadding PaddingLeft={new UDim(0, 10)} PaddingRight={new UDim(0, 10)} />
          </textbox>
        </TextField.Input>

        <textlabel
          BackgroundTransparency={1}
          Size={UDim2.fromOffset(240, 20)}
          Text={draft === saved ? "Saved" : "Unsaved changes"}
          TextColor3={Color3.fromRGB(170, 179, 195)}
          TextXAlignment={Enum.TextXAlignment.Left}
        />
      </frame>
    </TextField.Root>
  );
}
```

### Validation with invalid and Message

Validate on commit rather than every keystroke, then flip `invalid` on the Root. `Message` reads
that flag from context and recolours to the error tone, and your own elements can branch on the same
state. Swapping `Message` for `Description` keeps the layout height stable.

```tsx title="UsernameField.tsx"
import { useState } from "@rbxts/react";
import { TextField } from "@lattice-ui/react-text-field";

export function UsernameField() {
  const [name, setName] = useState("");
  const [error, setError] = useState(false);

  return (
    <TextField.Root
      value={name}
      onValueChange={setName}
      onValueCommit={(committed) => setError(committed.size() < 3)}
      invalid={error}
      required
      name="username"
    >
      <frame BackgroundTransparency={1} Size={UDim2.fromOffset(240, 88)}>
        <uilistlayout Padding={new UDim(0, 4)} SortOrder={Enum.SortOrder.LayoutOrder} />

        <TextField.Input asChild>
          <textbox PlaceholderText="Enter a name" Size={UDim2.fromOffset(240, 36)} TextXAlignment={Enum.TextXAlignment.Left}>
            <uipadding PaddingLeft={new UDim(0, 10)} PaddingRight={new UDim(0, 10)} />
          </textbox>
        </TextField.Input>

        {error ? (
          <TextField.Message asChild>
            <textlabel
              BackgroundTransparency={1}
              Size={UDim2.fromOffset(240, 20)}
              Text="At least 3 characters."
              TextXAlignment={Enum.TextXAlignment.Left}
            />
          </TextField.Message>
        ) : (
          <TextField.Description asChild>
            <textlabel
              BackgroundTransparency={1}
              Size={UDim2.fromOffset(240, 20)}
              Text="Visible to other players."
              TextXAlignment={Enum.TextXAlignment.Left}
            />
          </TextField.Description>
        )}
      </frame>
    </TextField.Root>
  );
}
```

### Disabled and readOnly

Both freeze the value but differ in interaction. `disabled` takes the input out of play — clearing
`Active`/`Selectable`, dimming the text, ignoring edits and suppressing the commit callback.
`readOnly` keeps the field focusable and copyable but rejects edits, and a focus loss still fires
`onValueCommit` with the unchanged text.

```tsx title="ServerInfoFields.tsx"
import { TextField } from "@lattice-ui/react-text-field";

export function ServerInfoFields(props: { joinCode: string; canEditRegion: boolean }) {
  return (
    <frame BackgroundTransparency={1} Size={UDim2.fromOffset(240, 80)}>
      <uilistlayout Padding={new UDim(0, 8)} SortOrder={Enum.SortOrder.LayoutOrder} />

      <TextField.Root value={props.joinCode} readOnly>
        <TextField.Input />
      </TextField.Root>

      <TextField.Root defaultValue="EU-West" disabled={!props.canEditRegion}>
        <TextField.Input />
      </TextField.Root>
    </frame>
  );
}
```

### Filtered input

For numeric-only entry, control the value and strip rejected characters in `onValueChange` before
they reach state; the `TextBox` text is bound to the owned value, so the field tracks the filtered
result. Run the same normalization at commit, which is where you parse and clamp.

```tsx title="BetAmountField.tsx"
import { useState } from "@rbxts/react";
import { TextField } from "@lattice-ui/react-text-field";

const MAX_BET = 500;

export function BetAmountField(props: { onBetChange: (amount: number) => void }) {
  const [amount, setAmount] = useState("0");

  return (
    <TextField.Root
      value={amount}
      onValueChange={(text) => {
        const [digits] = text.gsub("%D", "");
        setAmount(digits);
      }}
      onValueCommit={(text) => {
        const [digits] = text.gsub("%D", "");
        const clamped = math.clamp(tonumber(digits) ?? 0, 0, MAX_BET);
        setAmount(tostring(clamped));
        props.onBetChange(clamped);
      }}
    >
      <TextField.Input asChild>
        <textbox PlaceholderText="0" Size={UDim2.fromOffset(120, 36)} TextXAlignment={Enum.TextXAlignment.Left}>
          <uipadding PaddingLeft={new UDim(0, 10)} PaddingRight={new UDim(0, 10)} />
        </textbox>
      </TextField.Input>
    </TextField.Root>
  );
}
```

## How it behaves

### Value state

Pass `value` and `onValueChange` to control it, or `defaultValue` to run uncontrolled. The owned
value is bound to the `TextBox`'s `Text`, so the display tracks state rather than whatever Roblox
last typed, and setting the same value again is a no-op.

### Change and commit

A keystroke calls `onValueChange`. A commit happens on `FocusLost` and calls `onValueCommit` with
the text as it stands. Commit is not a diff: it fires on every focus loss, including when the text
is unchanged, and is suppressed only while `disabled` — a `readOnly` field still commits.

### Disabled and readOnly

Both stop edits from updating the value: `Root.setValue` ignores incoming text and the `Input`
rewrites the `TextBox` back to the owned value, so stray platform input cannot desync the field.
`disabled` also clears `Active`/`Selectable`, dims the text and suppresses the commit callback;
`readOnly` only drops `TextEditable`. `Input` can set either locally, combining with `Root`'s state
via OR.

### Required, invalid, and name

Both are shared flags with no enforcement of their own: `required` is exposed on context for your
validation logic, `invalid` is a semantic marker that makes `TextField.Message` recolour to the
error tone. `Description` never recolours for validity, and `name` is passed through context and
otherwise inert.

### Label and focus

`TextField.Label` renders a `textbutton` whose activation calls `CaptureFocus()` on the input, so
clicking the label focuses the field; while disabled it drops `Active`/`Selectable` and does
nothing. `Description` and `Message` are non-interactive labels that dim alongside a disabled field.

### Input focus motion

The `Input` tracks focus and exposes it through context, but paints nothing. Focus while `disabled`
or `readOnly` does not count as active, so a read-only field should not light up as editable —
branch on the state yourself, and add a `createFieldResponseRecipe()` if you want the change to
ease.

<Callout title="Default text and visuals">
Every part renders unstyled and carries no copy. `Input` is a `textbox` with `ClearTextOnFocus` off and no size or placeholder of its own; `Label`, `Description` and `Message` render nothing until you supply it.

Pass the copy and styling as props, or use `asChild` for a different element class. All four render children, so a `uipadding` or `uicorner` attaches directly.
</Callout>

<Callout title="Commit fires on every focus loss">
`onValueCommit` is not change detection — it fires whenever editing ends, even on identical text, and only `disabled` suppresses it. If you persist on commit, dedupe against the last saved value yourself.
</Callout>

<Callout title="Input children must be a TextBox">
With `asChild`, the `Input` merges its `Text` binding, editability flags and handlers onto your single child, and its ref wiring only accepts a `TextBox`. A `textlabel` or `frame` will not report changes or focus.
</Callout>

## API reference

### TextField.Root

<PropTable
  rows={[
    { name: "value", type: "string", description: "Controlled value. Pair with onValueChange." },
    { name: "defaultValue", type: "string", description: "Initial value for uncontrolled usage. Defaults to an empty string." },
    { name: "onValueChange", type: "(value: string) => void", description: "Called on every text change while the field is editable. Not called when the new text equals the current value." },
    { name: "onValueCommit", type: "(value: string) => void", description: "Called with the box's final text when editing ends (focus lost via Enter or clicking away). Fires even when the text is unchanged; suppressed while disabled." },
    { name: "disabled", type: "boolean", description: "Blocks edits, clears Active/Selectable, dims the input, and suppresses commit. Defaults to false." },
    { name: "readOnly", type: "boolean", description: "Blocks edits while keeping the field selectable and focusable; commit still fires. Defaults to false." },
    { name: "required", type: "boolean", description: "Shared flag exposed on context for your own validation wiring; not enforced. Defaults to false." },
    { name: "invalid", type: "boolean", description: "Marks the field invalid and recolors the Message part to the error tone. Defaults to false." },
    { name: "name", type: "string", description: "Identifier passed through context for form usage. No behavior of its own." },
    { name: "children", type: "React.ReactNode", description: "The field parts." },
  ]}
/>

### TextField.Input

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge input behavior onto the single child element instead of the textbox the part renders. The child must be a textbox." },
    { name: "disabled", type: "boolean", description: "Local disabled state, combined with Root's via OR. Defaults to false." },
    { name: "readOnly", type: "boolean", description: "Local readOnly state, combined with Root's via OR. Defaults to false." },
    { name: "children", type: "React.ReactElement", description: "The textbox element to render. Required when asChild is set." },
  ]}
/>

### TextField.Label

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge label behavior (focus-on-activate, disabled state) onto the single child element instead of the textbutton the part renders." },
    { name: "children", type: "React.ReactElement", description: "The element to render. Required when asChild is set." },
  ]}
/>

### TextField.Description

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge the description onto the single child element instead of the textlabel the part renders. Pass Text — the part renders no copy of its own." },
    { name: "children", type: "React.ReactElement", description: "The element to render. Required when asChild is set." },
  ]}
/>

### TextField.Message

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge the message onto the single child element instead of the textlabel the part renders. Pass Text — the part renders no copy of its own." },
    { name: "children", type: "React.ReactElement", description: "The element to render. Required when asChild is set." },
  ]}
/>

## Related

- [Controlled state](/lattice-ui/guides/controlled-state/)
- [Focus management](/lattice-ui/guides/focus-management/)
- [asChild composition](/lattice-ui/guides/as-child-composition/)
- [Roblox UI constraints](/lattice-ui/guides/roblox-ui-constraints/)
