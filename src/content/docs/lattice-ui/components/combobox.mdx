---
title: Combobox
description: Single-value selection with input filtering, item registration and popper positioning.
section: components
sidebar:
  order: 70
  group: Form
---

import { PackageMeta, PropTable, Callout, PrimitiveAnatomy , LoomPreview } from "@/components/docs/mdx";

<PackageMeta
  packageName="@lattice-ui/react-combobox"
  status="stable-direction"
  importName="Combobox"
  dependencies={["@lattice-ui/react-runtime", "@lattice-ui/react-layer", "@lattice-ui/react-motion", "@lattice-ui/react-popper"]}
/>

Combobox is the primitive for a filterable, single-value picker: searchable selects, command
palettes, autocomplete fields. It owns three pieces of state and keeps them in sync: the selected
**value**, the **input text** and the **open** state. It also repairs a selection that no longer
resolves to an enabled item.

## Preview

<LoomPreview scene="ComboboxExampleScene" height={400} />

## Import

```ts
import { Combobox } from "@lattice-ui/react-combobox";
```

## Anatomy

`Root`, `Portal`, `Content` and `Item` form the working picker. Use either `Input` (type-to-filter)
or `Trigger` + `Value` as the anchor, and `Group`, `Label` and `Separator` to structure longer
lists.

<PrimitiveAnatomy title="Combobox anatomy">

```tsx title="Combobox anatomy"
<Combobox.Root>
  <Combobox.Trigger>
    <Combobox.Value />
  </Combobox.Trigger>
  <Combobox.Input />
  <Combobox.Portal>
    <Combobox.Content>
      <Combobox.Label />
      <Combobox.Group>
        <Combobox.Item value="..." />
      </Combobox.Group>
      <Combobox.Separator />
      <Combobox.Item value="..." />
    </Combobox.Content>
  </Combobox.Portal>
</Combobox.Root>
```

| Part | Required | Responsibility |
| --- | --- | --- |
| `Combobox.Root` | yes | Owns value, input, and open state, plus the item registry and filter function. |
| `Combobox.Input` | no | A `TextBox` that opens the list on focus and drives the query as the user types. Acts as the anchor. |
| `Combobox.Trigger` | no | A button that toggles the list and acts as the anchor when no input is present. |
| `Combobox.Value` | no | A label that displays the selected item's text (or a placeholder). |
| `Combobox.Portal` | yes | Renders the listbox into a `ScreenGui` outside the local tree. |
| `Combobox.Content` | yes | The positioned, dismissable, motion-driven listbox. |
| `Combobox.Item` | yes | A selectable option that registers its value and text, and hides itself when filtered out. |
| `Combobox.Group` | no | A container that visually groups related items. |
| `Combobox.Label` | no | A non-interactive heading for a group or section. |
| `Combobox.Separator` | no | A thin divider between items or groups. |

</PrimitiveAnatomy>

## Examples

### Basic filtered list

An uncontrolled root, an input textbox as the anchor, and items on a plain list surface. Typing
narrows the list in place. Selecting sets the value, fills the field and closes the list.

`textValue` does not render as the item's label, so each item is given a `Text` of its own. It still
drives filtering and the resolved value label, and defaults to `value`.

```tsx title="BiomePicker.tsx"
import { Combobox } from "@lattice-ui/react-combobox";

const BIOMES = ["Ashlands", "Frostpeak", "Gladewood", "Mirelow", "Sunspire"];

export function BiomePicker() {
  return (
    <Combobox.Root defaultValue="Gladewood" onValueChange={(value) => print(`picked: ${value}`)}>
      <Combobox.Input placeholder="Search biomes" />

      <Combobox.Portal>
        <Combobox.Content sideOffset={4}>
          <frame
            AutomaticSize={Enum.AutomaticSize.Y}
            BackgroundColor3={Color3.fromRGB(28, 32, 42)}
            Size={UDim2.fromOffset(240, 0)}
          >
            <uilistlayout Padding={new UDim(0, 2)} SortOrder={Enum.SortOrder.LayoutOrder} />
            {BIOMES.map((biome) => (
              <Combobox.Item key={biome} Text={biome} value={biome} />
            ))}
          </frame>
        </Combobox.Content>
      </Combobox.Portal>
    </Combobox.Root>
  );
}
```

### Controlled value and input

The two are independent: `value`/`onValueChange` tracks the committed selection,
`inputValue`/`onInputValueChange` the field text. Expect `onInputValueChange` to fire for
programmatic syncs too — selecting sets the input to the item's text, and closing re-syncs it from
the value.

```tsx title="QuestSearch.tsx"
import { useState } from "@rbxts/react";
import { Combobox } from "@lattice-ui/react-combobox";

const QUESTS = ["Cinder Trial", "Echo Vault", "Gale Run", "Hollow March"];

export function QuestSearch() {
  const [value, setValue] = useState<string>();
  const [inputValue, setInputValue] = useState("");

  return (
    <frame BackgroundTransparency={1} Size={UDim2.fromOffset(260, 80)}>
      <uilistlayout Padding={new UDim(0, 8)} SortOrder={Enum.SortOrder.LayoutOrder} />

      <Combobox.Root
        value={value}
        onValueChange={setValue}
        inputValue={inputValue}
        onInputValueChange={setInputValue}
      >
        <Combobox.Input placeholder="Search quests" />

        <Combobox.Portal>
          <Combobox.Content sideOffset={4}>
            <frame
              AutomaticSize={Enum.AutomaticSize.Y}
              BackgroundColor3={Color3.fromRGB(28, 32, 42)}
              Size={UDim2.fromOffset(240, 0)}
            >
              <uilistlayout Padding={new UDim(0, 2)} SortOrder={Enum.SortOrder.LayoutOrder} />
              {QUESTS.map((quest) => (
                <Combobox.Item key={quest} Text={quest} value={quest} />
              ))}
            </frame>
          </Combobox.Content>
        </Combobox.Portal>
      </Combobox.Root>

      <textlabel
        BackgroundTransparency={1}
        Size={UDim2.fromOffset(260, 18)}
        Text={value !== undefined ? `Tracking: ${value}` : "No quest tracked"}
        TextColor3={Color3.fromRGB(168, 176, 191)}
        TextXAlignment={Enum.TextXAlignment.Left}
      />
    </frame>
  );
}
```

<Callout title="Uncontrolled usage">
Pass `defaultValue`, `defaultInputValue` or `defaultOpen` to let Combobox own its state. All three are independently controllable — control only what something outside needs to drive.
</Callout>

### Custom filter function

The root's `filterFn` decides whether each item matches the query, defaulting to a case-insensitive
substring match. Define it at module scope or memoize it — it is shared through context, and a new
function every render churns every item.

```tsx title="CommandPalette.tsx"
import { Combobox } from "@lattice-ui/react-combobox";
import type { ComboboxFilterFn } from "@lattice-ui/react-combobox";

const COMMANDS = ["Teleport", "Trade", "Track quest", "Toggle HUD"];

const prefixFilter: ComboboxFilterFn = (itemText, query) => {
  return string.sub(string.lower(itemText), 1, query.size()) === string.lower(query);
};

export function CommandPalette() {
  return (
    <Combobox.Root filterFn={prefixFilter} onValueChange={(command) => print(`run: ${command}`)}>
      <Combobox.Input placeholder="Type a command" />

      <Combobox.Portal>
        <Combobox.Content sideOffset={4}>
          <frame
            AutomaticSize={Enum.AutomaticSize.Y}
            BackgroundColor3={Color3.fromRGB(28, 32, 42)}
            Size={UDim2.fromOffset(240, 0)}
          >
            <uilistlayout Padding={new UDim(0, 2)} SortOrder={Enum.SortOrder.LayoutOrder} />
            {COMMANDS.map((command) => (
              <Combobox.Item key={command} Text={command} value={command} />
            ))}
          </frame>
        </Combobox.Content>
      </Combobox.Portal>
    </Combobox.Root>
  );
}
```

### Groups and labels

`Group`, `Label` and `Separator` are purely visual. They do not affect filtering or selection, and a
label stays visible even when every item under it is filtered out. All three render unstyled.

```tsx title="ServerRegionPicker.tsx"
import { Combobox } from "@lattice-ui/react-combobox";

const AMERICAS = ["Chicago", "Dallas", "Sao Paulo"];
const EUROPE = ["Frankfurt", "London", "Warsaw"];

export function ServerRegionPicker() {
  return (
    <Combobox.Root>
      <Combobox.Input placeholder="Search regions" />

      <Combobox.Portal>
        <Combobox.Content sideOffset={4}>
          <frame
            AutomaticSize={Enum.AutomaticSize.Y}
            BackgroundColor3={Color3.fromRGB(28, 32, 42)}
            Size={UDim2.fromOffset(240, 0)}
          >
            <uilistlayout Padding={new UDim(0, 2)} SortOrder={Enum.SortOrder.LayoutOrder} />

            <Combobox.Group asChild>
              <frame
                AutomaticSize={Enum.AutomaticSize.Y}
                BackgroundTransparency={1}
                Size={UDim2.fromOffset(240, 0)}
              >
                <uilistlayout Padding={new UDim(0, 2)} SortOrder={Enum.SortOrder.LayoutOrder} />
                <Combobox.Label asChild>
                  <textlabel
                    BackgroundTransparency={1}
                    Size={UDim2.fromOffset(240, 18)}
                    Text="Americas"
                    TextColor3={Color3.fromRGB(168, 176, 191)}
                    TextSize={13}
                    TextXAlignment={Enum.TextXAlignment.Left}
                  />
                </Combobox.Label>
                {AMERICAS.map((region) => (
                  <Combobox.Item key={region} Text={region} value={region} />
                ))}
              </frame>
            </Combobox.Group>

            <Combobox.Separator />

            <Combobox.Group asChild>
              <frame
                AutomaticSize={Enum.AutomaticSize.Y}
                BackgroundTransparency={1}
                Size={UDim2.fromOffset(240, 0)}
              >
                <uilistlayout Padding={new UDim(0, 2)} SortOrder={Enum.SortOrder.LayoutOrder} />
                <Combobox.Label asChild>
                  <textlabel
                    BackgroundTransparency={1}
                    Size={UDim2.fromOffset(240, 18)}
                    Text="Europe"
                    TextColor3={Color3.fromRGB(168, 176, 191)}
                    TextSize={13}
                    TextXAlignment={Enum.TextXAlignment.Left}
                  />
                </Combobox.Label>
                {EUROPE.map((region) => (
                  <Combobox.Item key={region} Text={region} value={region} />
                ))}
              </frame>
            </Combobox.Group>
          </frame>
        </Combobox.Content>
      </Combobox.Portal>
    </Combobox.Root>
  );
}
```

### Empty state when nothing matches

Filtering hides items one by one, so the listbox stays open even when nothing matches, and there is
no built-in empty-state part. To show "no matches", control `inputValue`, mirror the filtering with
the exported `filterComboboxOptions`, and render your own label when the count hits zero.

```tsx title="SpellSearch.tsx"
import { useState } from "@rbxts/react";
import { Combobox, filterComboboxOptions } from "@lattice-ui/react-combobox";

const SPELLS = ["Arc Nova", "Ember Coil", "Frost Lattice", "Stone Ward"];
const SPELL_OPTIONS = SPELLS.map((spell) => ({ value: spell, disabled: false, textValue: spell }));

export function SpellSearch() {
  const [inputValue, setInputValue] = useState("");
  const matchCount = filterComboboxOptions(SPELL_OPTIONS, inputValue).size();

  return (
    <Combobox.Root inputValue={inputValue} onInputValueChange={setInputValue}>
      <Combobox.Input placeholder="Search spells" />

      <Combobox.Portal>
        <Combobox.Content sideOffset={4}>
          <frame
            AutomaticSize={Enum.AutomaticSize.Y}
            BackgroundColor3={Color3.fromRGB(28, 32, 42)}
            Size={UDim2.fromOffset(240, 0)}
          >
            <uilistlayout Padding={new UDim(0, 2)} SortOrder={Enum.SortOrder.LayoutOrder} />
            {SPELLS.map((spell) => (
              <Combobox.Item key={spell} Text={spell} value={spell} />
            ))}
            {matchCount === 0 && (
              <textlabel
                BackgroundTransparency={1}
                Size={UDim2.fromOffset(240, 32)}
                Text="No spells match"
                TextColor3={Color3.fromRGB(153, 161, 177)}
              />
            )}
          </frame>
        </Combobox.Content>
      </Combobox.Portal>
    </Combobox.Root>
  );
}
```

### Custom input styling with asChild

Use `asChild` on `Combobox.Input` to project the input behaviour onto your own `textbox`. The
primitive drives `Text`, `TextEditable`, `PlaceholderText`, `ClearTextOnFocus`, the change handler
and the anchor ref. Keep `placeholder` on the part, and put your styling in colours, strokes,
corners and padding.

```tsx title="StyledCosmeticSearch.tsx"
import { Combobox } from "@lattice-ui/react-combobox";

const COSMETICS = ["Aurora Cape", "Drift Visor", "Ember Trail", "Void Crown"];

export function StyledCosmeticSearch() {
  return (
    <Combobox.Root>
      <Combobox.Input placeholder="Search cosmetics" asChild>
        <textbox
          BackgroundColor3={Color3.fromRGB(24, 26, 32)}
          BorderSizePixel={0}
          Size={UDim2.fromOffset(260, 40)}
          TextColor3={Color3.fromRGB(236, 241, 249)}
          TextSize={15}
          TextXAlignment={Enum.TextXAlignment.Left}
        >
          <uicorner CornerRadius={new UDim(0, 8)} />
          <uistroke Color={Color3.fromRGB(88, 142, 255)} Thickness={1} />
          <uipadding PaddingLeft={new UDim(0, 12)} PaddingRight={new UDim(0, 12)} />
        </textbox>
      </Combobox.Input>

      <Combobox.Portal>
        <Combobox.Content sideOffset={6}>
          <frame
            AutomaticSize={Enum.AutomaticSize.Y}
            BackgroundColor3={Color3.fromRGB(28, 32, 42)}
            Size={UDim2.fromOffset(260, 0)}
          >
            <uicorner CornerRadius={new UDim(0, 8)} />
            <uilistlayout Padding={new UDim(0, 2)} SortOrder={Enum.SortOrder.LayoutOrder} />
            {COSMETICS.map((cosmetic) => (
              <Combobox.Item key={cosmetic} Text={cosmetic} value={cosmetic} />
            ))}
          </frame>
        </Combobox.Content>
      </Combobox.Portal>
    </Combobox.Root>
  );
}
```

## How it behaves

### Open state

Pass `open` and `onOpenChange` to control the listbox, or `defaultOpen` to run uncontrolled.
`Trigger` toggles on activation and on `Return`/`Space`. `Input` opens the list on focus, where an
empty query matches every item, and typing keeps it open. Selecting closes it, and a disabled root
refuses to open but can still close.

### Value and input state

Value and input text are separate, independently controllable state. Selecting sets the value and
syncs the input to that item's display text, ignoring a disabled item. Typing updates the query and
opens the list without changing the value. Closing re-syncs the input from the value.

While the list is open, a selected value that no longer resolves to an enabled registered item is
replaced with the first enabled one. The repair never *fills* an empty selection, and
`onValueChange` fires only for defined values.

`Combobox.Value` displays the selected item's text from the registry, or its `placeholder`.
`disabled` blocks all state changes. `readOnly` blocks input edits but still allows selection
through items.

### Filtering

Filtering is per-item: each item evaluates the root's `filterFn(textValue, query)` and a
non-matching one sets its own `Visible` to false and becomes non-interactive, under `asChild` too.
The content never removes or reorders nodes, so your layout collapses around hidden items.

The active query is what the user typed. Selecting syncs the field text without changing the open
list's query, so the list does not collapse to the selected item. Closing re-syncs the query from
the field text, so reopening shows the list filtered by the selection.

The default filter is a case-insensitive substring match. `defaultComboboxFilter`,
`filterComboboxOptions` and `resolveComboboxInputValue` are exported as plain functions for
mirroring the filtering in your own logic.

### Positioning

`Combobox.Content` is positioned by the popper foundation against the active anchor, the input when
present and otherwise the trigger. It flips to the opposite side on collision. Tune it with
`placement` (default `"bottom"`), `sideOffset` (default `0`), `alignOffset` (default `0`) and
`collisionPadding` (default `8`).

### Dismissal

`Combobox.Content` is always **non-modal**, so the rest of the UI stays interactive. The trigger and
input are registered as inside refs. Any other outside press closes the list. Use
`onPointerDownOutside` and `onInteractOutside` to observe or veto first.

### Motion and presence

`Combobox.Content` runs no motion of its own. Pass a `transition` to animate it — `createPopperEntranceRecipe(placement)` matches the `frame` the content renders and animates from the resolved placement. `forceMount` keeps the content mounted through its exit (useful when you drive motion yourself or need the node to persist).

<Callout title="Three states, kept in sync">
Combobox reconciles the three automatically. Selecting syncs the input, closing re-syncs it from the value, and an invalid selection is repaired against the registry while the list is open. Mixing a controlled value with an uncontrolled input is fully supported.
</Callout>

<Callout title="Items hide themselves — there is no built-in empty state">
Each item toggles its own visibility, so the listbox stays open and mounted even when nothing matches. Detect zero matches with `filterComboboxOptions` over your own option data and render your own label — see the [empty state example](#empty-state-when-nothing-matches).
</Callout>

<Callout title="required is wiring-only">
`required` is exposed on context for your own validation wiring and changes no interaction. The value repair runs regardless and never fills an empty value, so validate `value !== undefined` yourself before submit.
</Callout>

## API reference

### Combobox.Root

<PropTable
  rows={[
    { name: "value", type: "string", description: "Controlled selected value. Pair with onValueChange." },
    { name: "defaultValue", type: "string", description: "Initial selected value for uncontrolled usage." },
    { name: "onValueChange", type: "(value: string) => void", description: "Called whenever the selected value changes. Fires only for defined values." },
    { name: "inputValue", type: "string", description: "Controlled input text. Pair with onInputValueChange." },
    { name: "defaultInputValue", type: "string", description: "Initial input text for uncontrolled usage. Defaults to an empty string." },
    { name: "onInputValueChange", type: "(inputValue: string) => void", description: "Called whenever the input text changes, including programmatic syncs from selection and close." },
    { name: "open", type: "boolean", description: "Controlled open state of the listbox. Pair with onOpenChange." },
    { name: "defaultOpen", type: "boolean", description: "Initial open state for uncontrolled usage. Defaults to false." },
    { name: "onOpenChange", type: "(open: boolean) => void", description: "Called whenever the open state changes." },
    { name: "disabled", type: "boolean", description: "Disables the whole combobox, blocking opening, input edits, and selection. Defaults to false." },
    { name: "readOnly", type: "boolean", description: "Blocks input edits while still allowing selection through items. Defaults to false." },
    { name: "required", type: "boolean", description: "Marks the combobox as required for your own form/validation wiring. Exposed on context; does not change interaction on its own. Defaults to false." },
    { name: "filterFn", type: "(itemText: string, query: string) => boolean", description: "Decides whether an item matches the query. Defaults to a case-insensitive substring match. Keep its identity stable." },
    { name: "children", type: "React.ReactNode", description: "The combobox parts." },
  ]}
/>

### Combobox.Input

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge input behavior onto the single child element instead of the textbox the part renders. The child must be a textbox." },
    { name: "disabled", type: "boolean", description: "Disables the input. Combined with the root's disabled state." },
    { name: "readOnly", type: "boolean", description: "Blocks edits to the input text. Combined with the root's readOnly state." },
    { name: "placeholder", type: "string", description: "Placeholder text shown when the input is empty. Defaults to \"Type to filter\"." },
    { name: "children", type: "React.ReactElement", description: "The element to render. Required when asChild is set." },
  ]}
/>

### Combobox.Trigger

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge trigger behavior onto the single child element instead of the textbutton the part renders." },
    { name: "disabled", type: "boolean", description: "Prevents the trigger from toggling the list. Combined with the root's disabled state." },
    { name: "children", type: "React.ReactElement", description: "The element to render. Required when asChild is set." },
  ]}
/>

### Combobox.Value

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge the resolved value onto the single child element instead of the textlabel the part renders." },
    { name: "placeholder", type: "string", description: "Text shown when no value is selected. Defaults to an empty string." },
    { name: "children", type: "React.ReactElement", description: "The element to render. Required when asChild is set." },
  ]}
/>

### Combobox.Portal

<PropTable
  rows={[
    { name: "container", type: "BasePlayerGui", description: "Target PlayerGui to render the listbox into. Defaults to the surrounding portal context's container." },
    { name: "displayOrderBase", type: "number", description: "Base DisplayOrder for the generated ScreenGui, used to order it against other layers. Defaults to the surrounding portal context's value." },
    { name: "children", type: "React.ReactNode", description: "The content part." },
  ]}
/>

### Combobox.Content

<PropTable
  rows={[
    { name: "placement", type: '"top" | "bottom" | "left" | "right"', description: "Requested side to position the listbox on. Flips on collision. Defaults to \"bottom\"." },
    { name: "sideOffset", type: "number", description: "Gap in pixels between the anchor and the listbox. Defaults to 0." },
    { name: "alignOffset", type: "number", description: "Shift in pixels along the anchor's cross axis. Defaults to 0." },
    { name: "collisionPadding", type: "number", description: "Minimum distance in pixels to keep from the screen edge. Defaults to 8." },
    { name: "asChild", type: "boolean", description: "Render the single child element inside the positioned wrapper instead of the frame the part renders." },
    { name: "forceMount", type: "boolean", description: "Keeps the listbox mounted while exit motion runs." },
    { name: "transition", type: "PresenceMotionConfig", description: "Reveal/exit motion. None by default; pass createPopperEntranceRecipe(placement) for a placement-aware entrance." },
    { name: "onPointerDownOutside", type: "(event: LayerInteractEvent) => void", description: "Called when a pointer press occurs outside the listbox, before dismissal." },
    { name: "onInteractOutside", type: "(event: LayerInteractEvent) => void", description: "Called for any other outside interaction, before dismissal." },
    { name: "children", type: "React.ReactNode", description: "The listbox contents." },
  ]}
/>

### Combobox.Item

<PropTable
  rows={[
    { name: "value", type: "string", description: "Required. The value selected when this item is chosen." },
    { name: "textValue", type: "string", description: "Text used for filtering and for the resolved value label. Defaults to value. Since 0.7.0 it does not render as the item's own label — supply that as a child or via a forwarded Text prop." },
    { name: "disabled", type: "boolean", description: "Prevents selection and excludes the item from open-list value repair." },
    { name: "asChild", type: "boolean", description: "Merge item behavior onto the single child element instead of the textbutton the part renders. The child's Visible property is bound to the filter match." },
    { name: "children", type: "React.ReactElement", description: "The element to render. Required when asChild is set." },
  ]}
/>

### Combobox.Group

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge the group onto the single child element instead of the frame the part renders." },
    { name: "children", type: "React.ReactElement", description: "The grouped items to render. Required when asChild is set." },
  ]}
/>

### Combobox.Label

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge the label onto the single child element instead of the textlabel the part renders." },
    { name: "children", type: "React.ReactElement", description: "The label element to render. Required when asChild is set." },
  ]}
/>

### Combobox.Separator

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge the separator onto the single child element instead of the frame the part renders." },
    { name: "children", type: "React.ReactElement", description: "The divider element to render. Required when asChild is set." },
  ]}
/>

## Related

- [Positioning with popper](/lattice-ui/guides/positioning-with-popper/)
- [Portals and layers](/lattice-ui/guides/portals-and-layers/)
- [Controlled state](/lattice-ui/guides/controlled-state/)
- [Presence and motion](/lattice-ui/guides/presence-and-motion/)
- [asChild composition](/lattice-ui/guides/as-child-composition/)
