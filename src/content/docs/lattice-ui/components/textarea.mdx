---
title: Textarea
description: Multi-line text input primitive that owns the value, separates change from commit, auto-resizes to its content within row bounds, and wires label, description, and message parts together.
section: components
sidebar:
  order: 90
  group: Form
---

import { PackageMeta, PropTable, Callout, PrimitiveAnatomy , LoomPreview } from "@/components/docs/mdx";

<PackageMeta
  packageName="@lattice-ui/react-textarea"
  status="stable-direction"
  importName="Textarea"
  dependencies={["@lattice-ui/react-runtime", "@lattice-ui/react-focus", "@lattice-ui/react-motion"]}
/>

Textarea wraps a multi-line Roblox `TextBox`: it owns the value, splits live changes from commit,
and grows its height to fit the content between configurable row bounds. Reach for it when an input
needs **controlled or uncontrolled value state**, a **change-versus-commit** split,
**auto-resizing**, and shared **disabled / readOnly / required / invalid** state across a label,
description and message. For single-line input, [Text Field](/lattice-ui/components/text-field/) is
the sibling primitive.

## Preview

<LoomPreview scene="TextareaExampleScene" height={360} />

## Import

```ts
import { Textarea } from "@lattice-ui/react-textarea";
```

The package also exports the pure height helper used internally:

```ts
import { resolveTextareaHeight } from "@lattice-ui/react-textarea";
```

## Anatomy

Compose `Root` around an `Input`, plus any of the optional text parts. Only `Root` and `Input` are required; `Label`, `Description`, and `Message` read shared state from context.

<PrimitiveAnatomy title="Textarea anatomy">

```tsx title="Textarea anatomy"
<Textarea.Root>
  <Textarea.Label />
  <Textarea.Input />
  <Textarea.Description />
  <Textarea.Message />
</Textarea.Root>
```

| Part | Required | Responsibility |
| --- | --- | --- |
| `Textarea.Root` | yes | Owns the value, commit callback, shared state, and the auto-resize bounds. |
| `Textarea.Input` | yes | The multi-line `TextBox` that renders the value, reports changes, and resizes to fit. |
| `Textarea.Label` | no | A `textbutton` that focuses the input when activated. |
| `Textarea.Description` | no | A static `textlabel` for helper text. |
| `Textarea.Message` | no | A `textlabel` for validation text that recolors when `invalid` is set. |

</PrimitiveAnatomy>

## Examples

### Basic labeled textarea

An uncontrolled field seeded with `defaultValue`, so the Root keeps the value and you hear about it
through the callbacks. `Label`, `Input` and `Description` each take `asChild`. Auto-resize is on by
default, so the `Size` you pass supplies the width and an initial height.

```tsx title="FeedbackField.tsx"
import { Textarea } from "@lattice-ui/react-textarea";

export function FeedbackField() {
  return (
    <Textarea.Root defaultValue="" onValueCommit={(value) => print(`feedback: ${value}`)}>
      <frame BackgroundTransparency={1} Size={UDim2.fromOffset(280, 160)}>
        <uilistlayout Padding={new UDim(0, 4)} SortOrder={Enum.SortOrder.LayoutOrder} />

        <Textarea.Label asChild>
          <textbutton
            AutoButtonColor={false}
            BackgroundTransparency={1}
            Size={UDim2.fromOffset(280, 22)}
            Text="Feedback"
            TextXAlignment={Enum.TextXAlignment.Left}
          />
        </Textarea.Label>

        <Textarea.Input asChild>
          <textbox PlaceholderText="Tell us what you think" Size={UDim2.fromOffset(280, 68)} TextSize={15}>
            <uipadding
              PaddingBottom={new UDim(0, 7)}
              PaddingLeft={new UDim(0, 10)}
              PaddingRight={new UDim(0, 10)}
              PaddingTop={new UDim(0, 7)}
            />
          </textbox>
        </Textarea.Input>

        <Textarea.Description asChild>
          <textlabel
            BackgroundTransparency={1}
            Size={UDim2.fromOffset(280, 20)}
            Text="Sent to the developers with your session info."
            TextXAlignment={Enum.TextXAlignment.Left}
          />
        </Textarea.Description>
      </frame>
    </Textarea.Root>
  );
}
```

### Auto-resize with row bounds

`minRows` and `maxRows` set the bounds. Here a composer starts at two rows and grows — from wrapped
lines as well as explicit newlines — until it hits six. The primitive rewrites only the height, so
give the input a fixed pixel width.

```tsx title="NoteComposer.tsx"
import { Textarea } from "@lattice-ui/react-textarea";

export function NoteComposer() {
  return (
    <Textarea.Root autoResize minRows={2} maxRows={6}>
      <Textarea.Input asChild>
        <textbox PlaceholderText="Write a note" Size={UDim2.fromOffset(260, 50)} TextSize={15}>
          <uipadding
            PaddingBottom={new UDim(0, 7)}
            PaddingLeft={new UDim(0, 10)}
            PaddingRight={new UDim(0, 10)}
            PaddingTop={new UDim(0, 7)}
          />
        </textbox>
      </Textarea.Input>
    </Textarea.Root>
  );
}
```

Put the input in a layout that tolerates height changes; a `uilistlayout` column reflows siblings
automatically. For the numbers without mounting anything, call `resolveTextareaHeight` with the same
options.

### Change vs commit

`onValueChange` fires on every text change; `onValueCommit` fires once when editing ends. Use change
for live UI — previews, counters, dirty indicators — and commit for the expensive work: saving,
validation, history.

```tsx title="ClanDescriptionField.tsx"
import { useState } from "@rbxts/react";
import { Textarea } from "@lattice-ui/react-textarea";

export function ClanDescriptionField(props: { onSave: (description: string) => void }) {
  const [draft, setDraft] = useState("A clan for casual raids.");
  const [saved, setSaved] = useState("A clan for casual raids.");

  return (
    <Textarea.Root
      value={draft}
      onValueChange={setDraft}
      onValueCommit={(committed) => {
        setSaved(committed);
        props.onSave(committed);
      }}
      minRows={2}
      maxRows={5}
    >
      <frame BackgroundTransparency={1} Size={UDim2.fromOffset(260, 120)}>
        <uilistlayout Padding={new UDim(0, 4)} SortOrder={Enum.SortOrder.LayoutOrder} />

        <Textarea.Input asChild>
          <textbox Size={UDim2.fromOffset(260, 50)} TextSize={15}>
            <uipadding
              PaddingBottom={new UDim(0, 7)}
              PaddingLeft={new UDim(0, 10)}
              PaddingRight={new UDim(0, 10)}
              PaddingTop={new UDim(0, 7)}
            />
          </textbox>
        </Textarea.Input>

        <textlabel
          BackgroundTransparency={1}
          Size={UDim2.fromOffset(260, 20)}
          Text={draft === saved ? "Saved" : "Unsaved changes"}
          TextColor3={Color3.fromRGB(170, 179, 195)}
          TextXAlignment={Enum.TextXAlignment.Left}
        />
      </frame>
    </Textarea.Root>
  );
}
```

### Validation with invalid and Message

Validate on commit rather than every keystroke, then flip `invalid` on the Root. `invalid` is a shared flag: the `Message` part reads it from context and recolors its text to the error tone, and your own elements can branch on the same state. Swapping `Message` for `Description` keeps the layout height stable while switching between helper and error copy.

```tsx title="ReportField.tsx"
import { useState } from "@rbxts/react";
import { Textarea } from "@lattice-ui/react-textarea";

export function ReportField() {
  const [text, setText] = useState("");
  const [error, setError] = useState(false);

  return (
    <Textarea.Root
      value={text}
      onValueChange={setText}
      onValueCommit={(committed) => setError(committed.size() === 0)}
      invalid={error}
      required
      minRows={3}
      maxRows={8}
      name="report"
    >
      <frame BackgroundTransparency={1} Size={UDim2.fromOffset(280, 160)}>
        <uilistlayout Padding={new UDim(0, 4)} SortOrder={Enum.SortOrder.LayoutOrder} />

        <Textarea.Label asChild>
          <textbutton
            AutoButtonColor={false}
            BackgroundTransparency={1}
            Size={UDim2.fromOffset(280, 22)}
            Text="What happened?"
            TextXAlignment={Enum.TextXAlignment.Left}
          />
        </Textarea.Label>

        <Textarea.Input asChild>
          <textbox PlaceholderText="Describe the issue" Size={UDim2.fromOffset(280, 68)} TextSize={15}>
            <uipadding
              PaddingBottom={new UDim(0, 7)}
              PaddingLeft={new UDim(0, 10)}
              PaddingRight={new UDim(0, 10)}
              PaddingTop={new UDim(0, 7)}
            />
          </textbox>
        </Textarea.Input>

        {error ? (
          <Textarea.Message asChild>
            <textlabel
              BackgroundTransparency={1}
              Size={UDim2.fromOffset(280, 20)}
              Text="A description is required."
              TextXAlignment={Enum.TextXAlignment.Left}
            />
          </Textarea.Message>
        ) : (
          <Textarea.Description asChild>
            <textlabel
              BackgroundTransparency={1}
              Size={UDim2.fromOffset(280, 20)}
              Text="Include as much detail as you can."
              TextXAlignment={Enum.TextXAlignment.Left}
            />
          </Textarea.Description>
        )}
      </frame>
    </Textarea.Root>
  );
}
```

### Character counter

For a length-limited field, control the value and truncate in `onValueChange` before the text
reaches state. The `TextBox` text is bound to the owned value, so anything past the limit never
appears, and the counter derives from the same state.

```tsx title="BioField.tsx"
import { useState } from "@rbxts/react";
import { Textarea } from "@lattice-ui/react-textarea";

const MAX_BIO = 200;

export function BioField() {
  const [bio, setBio] = useState("");

  return (
    <Textarea.Root
      value={bio}
      onValueChange={(text) => setBio(text.sub(1, MAX_BIO))}
      onValueCommit={(committed) => print(`bio saved: ${committed}`)}
      minRows={3}
      maxRows={6}
      name="bio"
    >
      <frame BackgroundTransparency={1} Size={UDim2.fromOffset(260, 130)}>
        <uilistlayout Padding={new UDim(0, 4)} SortOrder={Enum.SortOrder.LayoutOrder} />

        <Textarea.Input asChild>
          <textbox PlaceholderText="Tell other players about yourself" Size={UDim2.fromOffset(260, 68)} TextSize={15}>
            <uipadding
              PaddingBottom={new UDim(0, 7)}
              PaddingLeft={new UDim(0, 10)}
              PaddingRight={new UDim(0, 10)}
              PaddingTop={new UDim(0, 7)}
            />
          </textbox>
        </Textarea.Input>

        <textlabel
          BackgroundTransparency={1}
          Size={UDim2.fromOffset(260, 18)}
          Text={`${bio.size()}/${MAX_BIO}`}
          TextColor3={bio.size() >= MAX_BIO ? Color3.fromRGB(255, 128, 128) : Color3.fromRGB(170, 179, 195)}
          TextXAlignment={Enum.TextXAlignment.Right}
        />
      </frame>
    </Textarea.Root>
  );
}
```

## How it behaves

### Value state

Pass `value` and `onValueChange` to control it, or `defaultValue` to run uncontrolled. The owned
value is bound to the `TextBox`'s `Text`, so the display tracks state rather than whatever Roblox
last typed, and setting the same value again is a no-op. The `TextBox` renders with `MultiLine` and
`TextWrapped` on, top-aligned text, and `ClearTextOnFocus` off.

### Change and commit

A keystroke calls `onValueChange`. A commit happens on `FocusLost` and calls `onValueCommit` with
the text as it stands. Commit is not a diff: it fires on every focus loss, including when the text
is unchanged, and is suppressed only while `disabled` — a `readOnly` field still commits.

### Auto-resize

`autoResize` defaults to `true`. After each text change — and whenever the value changes externally — the `Input` measures its content and sets its height to `rows × lineHeight + verticalPadding`:

- **Rows** is the larger of the newline count and the wrapped measurement (`TextBounds.Y` over the line height, rounded up), floored at 1 and clamped between the bounds. `minRows` defaults to `3`; `maxRows`, when set, is raised to at least `minRows`.
- **Line height** defaults to `ceil(TextSize × 1.2)` unless you pass an explicit `lineHeight` on `Input`.
- **Vertical padding** is summed from the input's `UIPadding` children, falling back to `14`.

Each measurement re-runs on a deferred frame, so wrapped `TextBounds` that settle late are picked
up. The resize writes `UDim2.fromOffset(currentWidth, height)`, replacing any scale-based sizing —
size the input in pixel offsets when auto-resize is on. Disabled and read-only inputs still
re-measure. Set `autoResize={false}` to own the height yourself.

The same math is exposed as the pure `resolveTextareaHeight(text, options)`, for computing a layout
without mounting the component.

### Disabled and readOnly

Both stop edits from updating the value: `Root.setValue` ignores incoming text and the `Input`
rewrites the `TextBox` back to the owned value, so stray platform input cannot desync the field.
They differ in interaction — `disabled` also clears `Active`/`Selectable`, dims the text and
suppresses the commit callback, while `readOnly` only drops `TextEditable`, keeping the field
selectable. `Input` can set either locally, combining with `Root`'s state via OR.

### Required, invalid, and name

Both are shared flags with no enforcement of their own: `required` is exposed on context for your
validation logic, `invalid` is a semantic marker that makes `Textarea.Message` recolor to the error
tone. `Description` never recolors for validity, and `name` is passed through context and otherwise
inert.

### Label and focus

`Textarea.Label` renders a `textbutton` whose activation calls `CaptureFocus()` on the input, so
clicking the label focuses the field; while disabled it drops `Active`/`Selectable` and does
nothing. `Description` and `Message` are non-interactive labels that dim alongside a disabled field.

### Input focus motion

The `Input` tracks focus and exposes it through context, but paints nothing. Focus while `disabled`
or `readOnly` does not count as active, so a read-only field should not light up as editable —
branch on the state yourself, and add a `createFieldResponseRecipe()` if you want the change to
ease.

<Callout title="Default text and visuals">
Every part renders unstyled and carries no copy. `Input` is a multi-line `textbox` with no size, placeholder or padding of its own, and `Label`, `Description` and `Message` render nothing until you supply it.

Pass the copy and styling as props, or use `asChild` for a different element class. All four parts render children, so a `uipadding` attaches directly.
</Callout>

<Callout title="Auto-resize owns the height">
With `autoResize` on, the primitive rewrites the input's `Size` on mount and after every change, keeping only your X offset. Size the width in pixels, treat the height as an initial value, and put the field in a layout that reflows — or set `autoResize={false}`.
</Callout>

<Callout title="Commit fires on every focus loss">
`onValueCommit` is not change detection — it fires whenever editing ends, even on identical text, and only `disabled` suppresses it. If you persist on commit, dedupe against the last saved value yourself.
</Callout>

<Callout title="Input children must be a TextBox">
With `asChild`, the `Input` merges its `Text` binding, flags, editability and handlers onto your single child, and its ref wiring only accepts a `TextBox`. A `textlabel` or `frame` will not report changes, focus or auto-resize.
</Callout>

## API reference

### Textarea.Root

<PropTable
  rows={[
    { name: "value", type: "string", description: "Controlled value. Pair with onValueChange." },
    { name: "defaultValue", type: "string", description: "Initial value for uncontrolled usage. Defaults to an empty string." },
    { name: "onValueChange", type: "(value: string) => void", description: "Called on every text change while the field is editable. Not called when the new text equals the current value." },
    { name: "onValueCommit", type: "(value: string) => void", description: "Called with the box's final text when editing ends (focus lost). Fires even when the text is unchanged; suppressed while disabled." },
    { name: "disabled", type: "boolean", description: "Blocks edits, clears Active/Selectable, dims the input, and suppresses commit. Defaults to false." },
    { name: "readOnly", type: "boolean", description: "Blocks edits while keeping the field selectable and focusable; commit still fires. Defaults to false." },
    { name: "required", type: "boolean", description: "Shared flag exposed on context for your own validation wiring; not enforced. Defaults to false." },
    { name: "invalid", type: "boolean", description: "Marks the field invalid and recolors the Message part to the error tone. Defaults to false." },
    { name: "name", type: "string", description: "Identifier passed through context for form usage. No behavior of its own." },
    { name: "autoResize", type: "boolean", description: "Grows the input height to fit its content within the row bounds. Defaults to true." },
    { name: "minRows", type: "number", description: "Minimum visible rows. Floored at 1. Defaults to 3." },
    { name: "maxRows", type: "number", description: "Maximum visible rows before the height stops growing. Raised to at least minRows when set. Unbounded by default." },
    { name: "children", type: "React.ReactNode", description: "The field parts." },
  ]}
/>

### Textarea.Input

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge input behavior onto the single child element instead of rendering the default multi-line textbox. The child must be a textbox." },
    { name: "disabled", type: "boolean", description: "Local disabled state, combined with Root's via OR. Defaults to false." },
    { name: "readOnly", type: "boolean", description: "Local readOnly state, combined with Root's via OR. Defaults to false." },
    { name: "lineHeight", type: "number", description: "Explicit per-row pixel height used by auto-resize. Defaults to ceil(TextSize × 1.2)." },
    { name: "children", type: "React.ReactElement", description: "The textbox element to render. Required when asChild is set." },
  ]}
/>

### Textarea.Label

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge label behavior (focus-on-activate, disabled state) onto the single child element instead of the textbutton the part renders." },
    { name: "children", type: "React.ReactElement", description: "The element to render. Required when asChild is set." },
  ]}
/>

### Textarea.Description

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge the description onto the single child element instead of the textlabel the part renders. Pass Text — the part renders no copy of its own." },
    { name: "children", type: "React.ReactElement", description: "The element to render. Required when asChild is set." },
  ]}
/>

### Textarea.Message

<PropTable
  rows={[
    { name: "asChild", type: "boolean", description: "Merge the message onto the single child element instead of the textlabel the part renders. Pass Text — the part renders no copy of its own." },
    { name: "children", type: "React.ReactElement", description: "The element to render. Required when asChild is set." },
  ]}
/>

## Related

- [Controlled state](/lattice-ui/guides/controlled-state/)
- [Focus management](/lattice-ui/guides/focus-management/)
- [asChild composition](/lattice-ui/guides/as-child-composition/)
- [Roblox UI constraints](/lattice-ui/guides/roblox-ui-constraints/)
