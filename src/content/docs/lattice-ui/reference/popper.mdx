---
title: Popper
description: Placement math, layout observers, and a positioning hook for anchoring a content surface to a GuiObject with viewport collision handling.
section: reference
sidebar:
  order: 50
---

import { PackageMeta, PropTable, Callout } from "@/components/docs/mdx";

<PackageMeta
  packageName="@lattice-ui/react-popper"
  status="experimental"
  dependencies={["@lattice-ui/react-runtime", "@lattice-ui/react-motion"]}
/>

Popper is the placement engine behind Popover, Tooltip, Menu and Select listboxes. Given an anchor
`GuiObject` and a content `GuiObject`, it computes where the content should sit, falls back to a
better side when the requested one would overflow, and keeps the result in sync as the anchor,
content or viewport changes.

It works in screen space through `AbsolutePosition` and `AbsoluteSize`, so it does not care how the
surface is composed — it returns a `UDim2` position and an `AnchorPoint` to apply to the content
node.

<Callout title="Experimental">
This package is experimental. The placement-relative offsets and collision handling are in active development, and option names, defaults and the shape of the computed result may change.
</Callout>

## Import

```ts
import { computePopper, usePopper } from "@lattice-ui/react-popper";
import { subscribeAnchor, subscribeContent, subscribeViewport } from "@lattice-ui/react-popper";
```

## API reference

### usePopper

The primary entry point: pass refs to the anchor and content `GuiObject`s and it tracks their
layout, recomputes on change, and returns a position and anchor point.

```ts
function usePopper(options: UsePopperOptions): UsePopperResult;

const { position, anchorPoint, placement, isPositioned } = usePopper({
  anchorRef,
  contentRef,
  placement: "bottom",
  sideOffset: 8,
});

return (
  <frame
    ref={contentRef}
    AnchorPoint={anchorPoint}
    Position={position}
    Visible={isPositioned}
  />
);
```

It measures the anchor's `AbsolutePosition`/`AbsoluteSize` and the content's `AbsoluteSize` and runs
`computePopper`, subscribing to anchor layout, content size and viewport resizes through the
observers below and reconciling the observed instances every `Heartbeat` so the right nodes stay
tracked as refs swap. Updates run in a layout effect, so positioning applies before paint.

Viewport bounds come from the content's nearest `ScreenGui` ancestor, honouring the topbar inset
unless `IgnoreGuiInset` is set, falling back to the camera's `ViewportSize`. Until the content has a
measured size `isPositioned` is `false` — gate the surface's `Visible` on it to avoid a one-frame
flash at the origin.

<PropTable
  rows={[
    { name: "anchorRef", type: "RefObject<GuiObject> | MutableRefObject<GuiObject | undefined>", description: "Ref to the element the surface anchors to. Its AbsolutePosition and AbsoluteSize drive placement." },
    { name: "contentRef", type: "RefObject<GuiObject> | MutableRefObject<GuiObject | undefined>", description: "Ref to the surface being positioned. Its AbsoluteSize is measured for collision math." },
    { name: "placement", type: "PopperPlacement", description: "Preferred side: \"top\", \"bottom\", \"left\", or \"right\". Defaults to \"bottom\"." },
    { name: "sideOffset", type: "number", description: "Gap between anchor and content along the placement axis. Defaults to 0." },
    { name: "alignOffset", type: "number", description: "Shift along the cross axis. Defaults to 0." },
    { name: "collisionPadding", type: "number", description: "Minimum distance kept between the content and the viewport edges. Defaults to 8." },
    { name: "enabled", type: "boolean", description: "When false, skips computation and detaches observers. Defaults to true." },
  ]}
/>

The returned `UsePopperResult` extends `ComputePopperResult` with the live measurement and an imperative escape hatch:

<PropTable
  rows={[
    { name: "position", type: "UDim2", description: "Offset-based position to apply to the content's Position." },
    { name: "anchorPoint", type: "Vector2", description: "AnchorPoint to apply to the content, derived from the resolved placement." },
    { name: "placement", type: "PopperPlacement", description: "The placement actually used after collision resolution, which may differ from the requested one." },
    { name: "contentSize", type: "Vector2", description: "Last measured AbsoluteSize of the content. Zero until first measured." },
    { name: "isPositioned", type: "boolean", description: "True once the content has a non-zero measured size and a real position has been computed." },
    { name: "update", type: "() => void", description: "Forces an immediate recompute. Rarely needed — observers cover normal changes." },
  ]}
/>

### computePopper

The pure placement function behind the hook: measured geometry in, a position, anchor point and
resolved placement out. No Roblox instances, refs or side effects.

```ts
function computePopper(input: ComputePopperInput): ComputePopperResult;

const { position, anchorPoint, placement } = computePopper({
  anchorPosition: anchor.AbsolutePosition,
  anchorSize: anchor.AbsoluteSize,
  contentSize: content.AbsoluteSize,
  viewportRect: new Rect(new Vector2(0, 0), camera.ViewportSize),
  placement: "bottom",
  sideOffset: 8,
});
```

It evaluates the requested placement, then its opposite, then the two orthogonal sides, scoring each
by viewport overflow plus a small penalty for an orthogonal fallback. The first zero-overflow
candidate wins; otherwise the least-bad one is clamped inside the viewport with `collisionPadding`.
The returned `position` is an offset `UDim2` already adjusted for the resolved `anchorPoint`.

`ComputePopperInput` merges the positioning options (`placement`, `sideOffset`, `alignOffset`, `collisionPadding`) with the required geometry:

<PropTable
  rows={[
    { name: "anchorPosition", type: "Vector2", description: "The anchor's top-left in screen space (AbsolutePosition)." },
    { name: "anchorSize", type: "Vector2", description: "The anchor's AbsoluteSize." },
    { name: "contentSize", type: "Vector2", description: "The content's AbsoluteSize. May be zero before the surface is measured." },
    { name: "viewportRect", type: "Rect", description: "The bounds to keep the content inside, in screen space." },
  ]}
/>

### subscribeAnchor

```ts
function subscribeAnchor(anchor: GuiObject, onChange: () => void): ObserverUnsubscribe;
```

Calls `onChange` when the anchor's `AbsolutePosition` or `AbsoluteSize` changes, returning an
unsubscribe that disconnects both signals.

### subscribeContent

```ts
function subscribeContent(content: GuiObject, onChange: () => void): ObserverUnsubscribe;
```

Calls `onChange` when the content's `AbsoluteSize` changes. Position changes are ignored
deliberately — the positioned ancestor owns placement, and relative content movement may be
motion-owned.

### subscribeViewport

```ts
function subscribeViewport(anchor: GuiObject | undefined, onChange: () => void): ObserverUnsubscribe;
```

Watches whatever defines the viewport bounds. If the anchor lives under a `ScreenGui`, it tracks that `ScreenGui`'s `AbsoluteSize`; otherwise it falls back to the current camera's `ViewportSize`, re-binding automatically when `Workspace.CurrentCamera` changes. Returns an unsubscribe function.

### Types

The package re-exports the types backing the API above:

<PropTable
  rows={[
    { name: "PopperPlacement", type: "\"top\" | \"bottom\" | \"left\" | \"right\"", description: "The four sides a surface can attach to." },
    { name: "PopperPositioningOptions", type: "object", description: "Optional placement, sideOffset, alignOffset, and collisionPadding." },
    { name: "NormalizedPopperPositioningOptions", type: "object", description: "The same options with every field resolved to a concrete value." },
    { name: "ComputePopperInput", type: "object", description: "Positioning options plus the measured geometry computePopper needs." },
    { name: "ComputePopperResult", type: "object", description: "The computed position, anchorPoint, and resolved placement." },
    { name: "UsePopperOptions", type: "object", description: "Positioning options plus anchorRef, contentRef, and enabled." },
    { name: "UsePopperResult", type: "object", description: "ComputePopperResult plus contentSize, isPositioned, and update." },
    { name: "ObserverUnsubscribe", type: "() => void", description: "The disconnect callback returned by every subscribe helper." },
  ]}
/>

<Callout title="Roblox bounds and the topbar inset">
Viewport bounds come from the nearest `ScreenGui` ancestor of the content, not the raw camera viewport. When that `ScreenGui` does not set `IgnoreGuiInset`, the topbar inset from `GuiService:GetGuiInset()` is folded into the bounds so surfaces are not clamped under the topbar. Render anchored surfaces inside a `ScreenGui` for accurate collision handling.
</Callout>

## Related links

- [Popover](/lattice-ui/components/popover/)
- [Tooltip](/lattice-ui/components/tooltip/)
- [Menu](/lattice-ui/components/menu/)
- [Portals and layers](/lattice-ui/guides/portals-and-layers/)
